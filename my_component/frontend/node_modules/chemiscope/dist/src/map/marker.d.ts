import { Indexes } from '../indexer';
export declare class MarkerData {
    color: string;
    current: number;
    marker: HTMLElement;
    constructor(guid: string, color: string, environment: number, visible?: boolean);
    update(position: {
        x: number;
        y: number;
    }): void;
    select(indexes: Indexes): boolean;
    activate(): void;
    deactivate(): void;
    remove(): void;
    toggleVisible(visible: boolean): void;
}
